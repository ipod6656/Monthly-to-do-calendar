---
description: Start the preview server
---

1. Start the Next.js development server:
   ```bash
   npx next dev --port 3000
   ```
2. Open the browser to [http://localhost:3000](http://localhost:3000) to view the application.
